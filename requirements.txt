# Core (Phase 0)
anthropic>=0.40.0
python-dotenv>=1.0.0
pillow>=10.0.0
opencv-python>=4.9.0

# Phase 2 — uncomment when wiring verbal/retro modes
# sounddevice>=0.4.6      # rolling audio buffer capture
# faster-whisper>=1.0.0   # local STT
# numpy>=1.26.0           # buffer math

# Phase 1 — uncomment when Frame hardware arrives
# frame-ble
# frame-msg

# Optional
# google-generativeai     # Gemini vision provider
# pytest                  # tests
