"""Coach providers — transcript + memory + intent → short HUD directive.

Sibling to vision/. Vision answers about an image; coach answers about a
conversation, a remembered fact, or a synthesized insight.
"""
